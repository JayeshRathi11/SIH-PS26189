fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0
sqlalchemy>=2.0.0
neo4j>=5.0.0
pandas>=2.0.0
networkx>=3.0
# Required by networkx.pagerank()'s default backend (_pagerank_scipy) --
# without it, GraphAnalyticsEngine.compute_pagerank() silently falls into
# its except-fallback and returns a uniform 1/N score for every entity
# instead of a real PageRank. Not previously listed here or installed in
# this env; discovered while migrating graph analytics to Neo4j GDS.
scipy>=1.11.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
pytest>=7.0.0
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
reportlab>=4.0.0
pypdf>=4.0.0
psycopg2-binary>=2.9.0
