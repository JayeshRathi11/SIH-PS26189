pandas>=2.0.0
pydantic>=2.0.0
rapidfuzz>=3.0.0
networkx>=3.0
# Required by networkx.pagerank()'s default backend (_pagerank_scipy) --
# without it, GraphAnalyticsEngine.compute_pagerank() silently falls into
# its except-fallback and returns a uniform 1/N score for every entity
# instead of a real PageRank. Not previously listed here or installed in
# this env; discovered while migrating graph analytics to Neo4j GDS.
scipy>=1.11.0
neo4j>=5.0.0
google-genai>=0.1.0
# Secondary extraction fallback when Gemini is rate-limited/unavailable --
# LLMExtractor._call_groq_api() imports this lazily. Was referenced in code
# but never actually listed here, so the "fallback" silently skipped straight
# to the deterministic regex extractor on every Gemini failure.
groq>=0.9.0
requests>=2.28.0
python-dotenv>=1.0.0
spacy>=3.5.0
pytest>=7.0.0
# Image OCR (extract_text_from_image() in pipeline/ingestion/parse_documents.py)
# now calls Sarvam AI's Document AI Digitise API over plain HTTP (see
# `requests` above) -- no separate SDK needed, since the whole flow is one
# multipart POST plus two polling GETs. Previously used surya-ocr, dropped
# after a direct accuracy comparison on real handwritten test data showed
# Sarvam correctly reading names Surya garbled (e.g. "Devendra Solanki" ->
# "Demudra Solanki" under Surya). Requires SARVAM_API_KEY -- see .env.example.
